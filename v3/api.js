(function(){
  const API_URL='https://script.google.com/macros/s/AKfycbw32BGSXwFVOpRCknx5hn8-k2m5ZXox26_y2mnZKVWL0JKHCv_Qtly5JiY0FS9e87kU/exec';
  const pending=new Map(),requestId=()=>`qv3_${Date.now().toString(36)}_${Math.random().toString(36).slice(2,8)}`;
  window.addEventListener('message',event=>{if(!(event.origin==='https://script.google.com'||event.origin.endsWith('.googleusercontent.com')))return;const data=event.data||{},request=pending.get(data.requestId);if(data.type!=='sunbot-pages-response'||!request)return;pending.delete(data.requestId);clearTimeout(request.timer);request.frame.remove();data.ok?request.resolve(data.result):request.reject(new Error(data.error||'Máy chủ không thể xử lý yêu cầu.'))});
  function bridge(mode,subaction,payload,token){return new Promise((resolve,reject)=>{const id=requestId(),frame=document.createElement('iframe');frame.name=id;frame.className='bridge-frame';document.body.appendChild(frame);const timer=setTimeout(()=>{pending.delete(id);frame.remove();reject(new Error('Kết nối máy chủ quá thời gian.'))},25000);pending.set(id,{resolve,reject,frame,timer});const form=document.createElement('form');form.method='POST';form.action=API_URL;form.target=id;form.className='bridge-form';const fields={action:'pagesBridge',request_id:id,mode,subaction,token:token||'',payload:JSON.stringify(payload||{})};Object.entries(fields).forEach(([name,value])=>{const input=document.createElement('input');input.type='hidden';input.name=name;input.value=value;form.appendChild(input)});document.body.appendChild(form);form.submit();form.remove()})}
  window.QuotationApi={login:(email,pin)=>bridge('pinLogin','',{email,pin},''),call:(token,action,payload)=>bridge('quotationV3',action,payload||{},token)};
})();
